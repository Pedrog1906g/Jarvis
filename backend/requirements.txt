fastapi==0.115.0
uvicorn==0.30.6
websockets==16.1.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.12
pydantic==2.12.0
pydantic-core==2.41.1
pydantic-settings==2.5.2
python-dotenv==1.0.1
PyJWT==2.9.0
httpx==0.27.2
python-multipart==0.0.12
openai==1.47.1
zeroconf>=0.115.0
cryptography>=44.0.0
psutil>=6.0.0

# Opcional: LLM Providers
ollama>=0.3.0  # Para usar Ollama localmente
anthropic>=0.34.0  # Para usar Anthropic Claude
google-generativeai>=0.8.0  # Para usar Google Gemini

# Opcional: Firebase Admin SDK. É pesado (puxa grpcio/cryptography/google-cloud-*).
# Só é necessário se você for usar Firebase. Veja requirements-firebase.txt.
# O app faz import lazy dele, então rodar SEM ele é seguro (Firebase fica desativado
# até você instalá-lo e definir as variáveis FIREBASE_* no Render).
