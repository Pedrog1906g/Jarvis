# App JARVIS para Windows — dependências
pystray>=0.19          # ícone na bandeja
Pillow>=10.0          # ícone/imagens
pyttsx3>=2.90         # TTS (voz do Windows / SAPI)
SpeechRecognition>=3.10  # STT (reconhecimento de voz)
websocket-client>=1.6 # chat em tempo real com o backend
requests>=2.31
psutil>=5.9           # monitor de desempenho
pywin32>=306; sys_platform == 'win32'   # controles do Windows (opcional)
pycaw>=0.6.0; sys_platform == 'win32'   # controle de volume (Windows)
comtypes>=1.2.0; sys_platform == 'win32'
pyaudio>=0.2.14; sys_platform == 'win32'   # microfone / reconhecimento de voz (STT)
